A User authentication system using Node.js, mongodb and passport.js.
After a user registers, a confirmation link is sent to the email that the user provided (in order to validate the user). 
A user cannot authenticate/login unless his account is validated first